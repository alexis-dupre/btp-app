---
topic: Epic E0 — walking skeleton: auth, organisation, multi-tenant data access layer, minimal client contact (particulier/professionnel), company settings with legal mentions
updated: 2026-09-13T17:26
---

- (note) Sources read: docs/product/devis-facture-direction.md (§4.7, §5, §6 E0), docs/standards/10-architecture.md, 30-security.md, 40-performance-scalability.md, 70-data-and-migrations.md, 00-definition-of-done.md, docs/domain/btp-rules.md, ADR-0001/0004/0006.
- (constraint) Scope fixed by the user: auth, organisation, tenancy layer, minimal client contact, company legal settings. No devis, no chantier, no PDF.
- (note) Repo is greenfield for this epic: app/ has layout+page only, lib/ has utils+domain stub, no database, no auth, no Drizzle or Postgres dependency in package.json. E0 introduces all of it.
- (question) Section 5.1 of the Costructor reverse-engineering spec (particulier/professionnel client fields) is cited by the user but is not in the repo — only devis-facture-direction.md is. Field set must be confirmed or assumed.
- (decision) Account creation: self-service signup creates the user and the organisation in one transaction; the signer becomes admin of that org. E0 screens: signup, login, logout.
- (decision) Roles: the full six-role enum of 30-security.md ships in the schema at E0 to avoid an enum migration later, but only 'admin' is assignable and tested in E0.
- (decision) MFA for admin/compta (30-security.md §5) is deferred out of E0 under its own ADR fixing the deadline. E0 ships password + server-revocable session + login rate-limit.
- (decision) Multi-tenancy model: shared schema with org_id + RLS, the documented default of 10-architecture.md. E0 records it as an ADR — 10-architecture.md lists it as a decision owed before the first line of feature code, and E0 is that first line.
- (note) Read both source devis (docs/product/sources/, pypdf). Client block printed: particulier = 'Stéphanie CAHN' / '13 rue Tronchet' / '75008 PARIS 08'; professionnel = 'AB COFFEE LAB' / '22 Rue Beaurepaire' / '75010 PARIS 10'. No email, no phone, and no client SIRET or TVA number on either document — not even for the company client.
- (note) Company legal block observed, header: SORENOV 75, 27 rue de la Bucaille 95510 AINCOURT, FRANCE, sorenov.75@outlook.com, TVA intracom FR94984350660, SIRET 98435066000010, Code NAF 4331Z, RCS 'Pontoise B 984 350 660', Capital 1 200 EUR. Footer adds legal form 'SAS SO RENOV 75', siège social, and 'Assurance décennale CBA Assurance, contrat numéro FR13-RCD24P01420'.
- (constraint) Defect found in both real devis: header prints Code NAF 4331Z, footer prints 4120B — the same legal identifier diverged because Henrri stores company info and footer text in two places (competitor spec §8.5 lists 'Pieds de page' as a separate setting). Product requirement for E0: each legal mention is entered once, stored once, and every rendering of it reads that single value. No free-text footer duplicating a structured field.
- (note) Competitor spec §5.1 read. Contact modal, tab Informations: toggle Particulier/Professionnel. Particulier = Civilité, Nom (requis), Prénom, Adresse(s), Email(s), Téléphone(s). Professionnel = Nom de la société (requis), N° SIREN/SIRET, N° de TVA, Adresse(s), Email(s), Téléphone(s), Interlocuteurs. Tab Comptabilité = Compte auxiliaire, Taux de TVA par défaut, Délai de paiement par défaut (30 j). Tab Autres = Notes, Responsable. §5.9 shows client SIREN/SIRET, téléphone and email are printable but off by default — which is why they are absent from SoRenov's devis.
- (decision) Client fields, tension resolved (user direction 2026-09-13): required to finalise a devis = type, display name (Nom for a particulier, Nom de la société for a professionnel) and postal address, because those are the only client data the two real devis print. Email and phone are collected and optional — they have a use inside the product's own scope (sending the devis, then the facture), which is a use and not speculation.
- (decision) Not collected in E0, recorded as known future needs: compte auxiliaire, taux de TVA par défaut, délai de paiement par défaut, interlocuteurs, notes, responsable, prospect-vs-client status, multiple addresses/emails/phones. Each has no consumer until the accounting, devis-defaults or CRM modules exist.
- (constraint) Client SIREN/SIRET is collected in E0 as an optional field on a professionnel, because the e-invoicing reform (R-001) makes the client's identification part of a structured invoice and the field must exist before E4 — but it is not required to save a contact until the register answers whether it is legally mandatory.
- (question) Is the client's SIREN/SIRET a mandatory mention on an invoice under the facturation électronique reform? Not in docs/domain/btp-rules.md, so it must not be asserted from memory. btp-domain-expert sources it and adds it to the register. Decides whether the field becomes required by E4.
- (question) 30-security.md §8 requires, 'from the start': export of a person's data, deletion with a retention override, and an audit trail of who read what. E0 creates the first personal data in the product — a client particulier is a named natural person with an address, an email and a phone. Either those three mechanisms are in E0, or their deferral is an ADR. The user scoped E0 as 'nothing else', so this spec excludes them and names the conflict rather than resolving it silently.
- (question) Is password reset in E0? Self-service signup without it strands the first pilot on a forgotten password, and 30-security.md §5 assumes the flow exists by requiring it be rate-limited. Excluded from this spec pending the answer.
- (question) docs/standards/10-architecture.md and ADR-0001 both name shadcn preset 'b4qO'; the running project reports preset 'b4nI' (style base-nova, neutral, Manrope). E0 is the first epic with real UI, so the drift surfaces now. One of the two must be corrected before design-system-guardian can arbitrate anything.
- (question) Realistic client-contact volume per organisation is not in 40-performance-scalability.md — its scale assumptions cover chantiers, pointages and devis lines, not contacts. The budget below uses a design target, not a sourced number.
- (constraint) Capital social is money: numeric(14,2) per 70-data-and-migrations.md, never a string and never a float. It is rendered '1 200 €' on the document but stored as a number.
- (decision) Two spec-authored companions: client-contact-fields.md (the field-by-field resolution across competitor §5.1 and the two devis) and company-legal-identity.md (the legal mentions, their observed values and the single-source rendering rule). Both are multi-item catalogs that would bloat the kernel.
- (event) Self-validate pass 1 (coherence, Spec Law 1-6 and 8): PASS. Six capabilities, each with intent and success. Twelve non-goals. Success signal is a runnable test plus a demonstrable data state. CAP-3's success names a TypeScript compile error, which is prescriptive — kept, because 10-architecture.md already mandates it and a weaker wording would not be testable.
- (event) Self-validate pass 2 (preservation): PASS. Every load-bearing claim landed. User scope statement -> CAP-1..6 and Non-goals. Direction §4.7 all seven legal mentions -> company-legal-identity.md. Direction §6 E0 including 'first two stories with a human in the loop' -> Why. Competitor §5.1 full field set -> client-contact-fields.md with a verdict per field. Both devis -> observed values and printed-client evidence. NAF divergence -> Constraints plus companion, as a product requirement. Wrapper-only content dropped on the record: competitor spec §§1-4, 5.2-5.11 and 6-14 (editor, margin engine, lifecycle, AI, competitor settings) and direction §§2,3,5,7 and E1-E6 — all out of E0 scope and owned by later epics.
- (event) Completeness gate (project on_complete): PASS. Five mandated elements present and substantive — tenant isolation impact, authorisation with nine predicate rows, performance budget with four surfaces, BTP rules applicability with three rules, non-empty non-goals. Placeholder scan 'grep -nE \{[A-Za-z<][^}]*\}' over SPEC.md and both companions returned nothing. No template comment leaked.
- (event) Build blockers carried out of the register into the spec: R-004 TO VERIFY (blocks any stored VAT rate — E0 stores none, so it blocks only the per-client default field we chose not to collect) and R-008 OPEN (blocks any deletion or RGPD retention capability, which is why none is in E0). R-001 is CONFIRMED and non-blocking.
- (decision) Password reset is IN scope for E0 (Alexis, 2026-09-13). Rationale recorded verbatim: it is a condition of signup existing, not a feature — self-service signup without reset fails on the first forgotten password. Supersedes the earlier open question and the non-goal.
- (decision) RGPD export / deletion / read audit trail: deferred under a dated ADR of the same shape as the MFA one. The trigger is observable, not a milestone: before the second paying organisation is onboarded, or on the first user request, whichever comes first. E0 still ships what makes the retrofit possible — a creation timestamp on every record holding personal data, and a stable identifier per data subject.
- (decision) shadcn preset drift: the project is right, the documents are wrong, and the fix targets the cause rather than the value. 10-architecture.md and ADR-0001 are updated to state that app/globals.css is the source of truth for tokens, not a preset code; b4nI is recorded as what the project resolved to; b4qO is recorded as the code passed to the CLI; both produce identical tokens, verified by 'shadcn apply --preset b4qO' changing only line ordering. A preset code is opaque transport and the CLI reference forbids decoding it by hand.
- (decision) The client SIREN/SIRET question leaves this spec. It is routed to btp-domain-expert and the register: it becomes a sourced rule there or it does not enter the model. The spec keeps the field optional and points at the register instead of carrying an open question.
- (decision) Client contact volume: a few hundred over the life of the company, not thousands (Alexis, describing the pilot). List budget sized on 500 contacts per organisation, 50 per page. This is an assumption drawn from a single pilot and the spec says so, to be revisited on the second customer.
- (note) Write boundaries checked before editing: this worktree is registered as stream 'main' (claims app, lib, components; exclusive deps). docs/standards, docs/adr and docs/specs are unclaimed, so they are writable here. docs/domain is claimed exclusively by the live 'domain' stream, so the R-009 register entry must be handed to that stream rather than written from here.
- (event) Preset drift fixed at the cause: 10-architecture.md drops the preset code from the stack table and points at app/globals.css; ADR-0001 gains a dated amendment recording b4qO as the code passed to the CLI, b4nI as what the project resolved to, the verification that both emit identical tokens, and the rule that app/globals.css settles any future disagreement. Note: docs/standards/20-design-system.md already carried the correct position ('the CLI re-emits it as b4nI; identical tokens', and a section headed 'The source of truth is app/globals.css, not the preset code'). The drift was confined to the two documents now corrected; they are aligned with the design-system standard rather than inventing a new position.
- (decision) Story breakdown agreed: seven stories in execution order — 1 tenancy scope, 2 accounts/sessions/signup, 3 password reset, 4 company legal identity, 5 client contact model+form, 6 client contact list, 7 E0 acceptance. Human checkpoints on 1, 2, 3 and 7 (Alexis, option 1), each carrying both spec_checkpoint and done_checkpoint.
- (decision) Story 7 must be written by a session that did not build stories 1 to 3 (Alexis). Reason: an agent writing the isolation suite for a boundary it built derives assertions from observed behaviour rather than from the contract, so the suite agrees with the implementation and a bug in the boundary becomes a passing test. Recorded in the story's invoke_dev_with.
- (event) stories.yaml written and checked against assets/stories-schema.md: 7 entries, ids unique, quoted strings, dash-and-alphanumeric only, prefix-free, no status field, every entry has title and description. VALID.
- (event) BLOCKED: scripts/loop/policy.txt could not be written. guard-bash.sh refuses it as part of the quality gate — 'an agent does not change the rules it is measured by' — and instructs to stop and ask the user. Not worked around. The checkpoint intent is carried in stories.yaml meanwhile; the loop halt itself needs the user to paste the block.
- (event) Second guard event, a false positive worth recording: writing stories.yaml by shell heredoc was refused because the invoke_dev_with text of story 1 contained the literal banned command string while warning a future agent against it. Content was scanned as if it were a command. Written with the Write tool instead, text preserved. ADR-0004 names false positives as the real risk of the Bash path guard; this is one.
- (note) btp-domain-expert returned a sourced R-009 block (client SIREN as a mandatory B2B invoice mention; CGI annexe II art. 242 nonies A 1°, décrets 2022-1299 and 2024-266, C. com. R. 123-221; calendar inherited from R-001, keyed to the ISSUER's size, 2026-09-01 large/ETI and 2027-09-01 TPE/PME; B2C excluded; sanction CGI art. 1737 II). Headline for this spec: the mandatory mention is the SIREN (9 digits), not the SIRET, and client type is load-bearing. One sub-point left TO VERIFY — whether a missing buyer SIREN causes outright rejection by the annuaire, which needs the DGFiP spécifications externes B2B v3.2 of 30/04/2026. docs/domain is owned by the 'domain' stream, so the block is handed over, not written here, and the spec cites no status until it lands.
